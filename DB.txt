CREATE TABLE CENSOS (
NOMBRE VARCHAR(250), 
NIF VARCHAR(9), 
EMAIL VARCHAR(250), 
CODCOLEGIOELECTORAL INTEGER, 
PASSWORD VARCHAR(20), 
CONSTRAINT PK_NIF PRIMARY KEY (NIF), 
CONSTRAINT UQ_EMAIL UNIQUE (NIF))